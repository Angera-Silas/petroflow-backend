# === Application ===
spring.application.name=petroflow-backend
spring.config.import=optional:file:.env[.properties]
app.data.init.enabled=${APP_DATA_INIT_ENABLED:false}
app.schema.init.enabled=${APP_SCHEMA_INIT_ENABLED:true}

# === Server ===
server.address=0.0.0.0
server.port=${PORT:8080}
server.servlet.context-path=/api
server.shutdown=graceful
server.servlet.session.timeout=30m

# === HTTP/2, Threads & Compression ===
server.http2.enabled=true
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=25
server.tomcat.accept-count=500
server.tomcat.max-connections=5000
server.tomcat.connection-timeout=20000
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/css,text/javascript
server.compression.min-response-size=1024

# === Database (Supabase PostgreSQL / Local PostgreSQL) ===
spring.datasource.url=${SPRING_DATASOURCE_URL:${DATABASE_URL:jdbc:postgresql://localhost:5432/petroflow_dev}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DATABASE_USER:postgres}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DATABASE_PASSWORD:postgres}}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pooling
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.initialization-fail-timeout=0

# === Hibernate/JPA ===
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:none}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_FORMAT_SQL:false}
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.open-in-view=false

# === Flyway ===
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:false}
spring.flyway.locations=${SPRING_FLYWAY_LOCATIONS:classpath:db/migration/shared}
spring.flyway.default-schema=${SPRING_FLYWAY_DEFAULT_SCHEMA:public}
spring.flyway.baseline-on-migrate=${SPRING_FLYWAY_BASELINE_ON_MIGRATE:true}
spring.flyway.validate-on-migrate=${SPRING_FLYWAY_VALIDATE_ON_MIGRATE:true}
spring.flyway.create-schemas=false
app.flyway.migrate-on-startup=${APP_FLYWAY_MIGRATE_ON_STARTUP:false}
app.flyway.shared-schema=${APP_FLYWAY_SHARED_SCHEMA:public}
app.flyway.shared-locations=${APP_FLYWAY_SHARED_LOCATIONS:classpath:db/migration/shared}
app.flyway.tenant-locations=${APP_FLYWAY_TENANT_LOCATIONS:classpath:db/migration/tenant}
app.flyway.baseline-on-migrate=${APP_FLYWAY_BASELINE_ON_MIGRATE:true}
app.flyway.validate-on-migrate=${APP_FLYWAY_VALIDATE_ON_MIGRATE:true}

# === GraphQL ===
spring.graphql.path=/graphql
spring.graphql.graphiql.enabled=${SPRING_GRAPHQL_GRAPHIQL_ENABLED:false}

# === Redis (optional by default) ===
spring.cache.type=${SPRING_CACHE_TYPE:simple}
spring.session.store-type=${SPRING_SESSION_STORE_TYPE:none}
spring.session.redis.namespace=petroflow:session
spring.data.redis.repositories.enabled=false
spring.data.redis.url=${REDIS_URL:redis://${SPRING_DATA_REDIS_HOST:${REDIS_HOST:localhost}}:${SPRING_DATA_REDIS_PORT:${REDIS_PORT:6379}}}
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:${REDIS_HOST:localhost}}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:${REDIS_PORT:6379}}
spring.data.redis.username=${SPRING_DATA_REDIS_USERNAME:${REDIS_USERNAME:}}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:${REDIS_PASSWORD:}}
spring.data.redis.ssl.enabled=${REDIS_SSL_ENABLED:false}
spring.data.redis.timeout=6000

# Jedis direct connectivity check (based on Redis FAQ example)
app.redis.jedis.enabled=${APP_REDIS_JEDIS_ENABLED:true}
app.redis.jedis.test-on-startup=${APP_REDIS_JEDIS_TEST_ON_STARTUP:true}
app.redis.jedis.host=${SPRING_DATA_REDIS_HOST:${REDIS_HOST:localhost}}
app.redis.jedis.port=${SPRING_DATA_REDIS_PORT:${REDIS_PORT:6379}}
app.redis.jedis.username=${SPRING_DATA_REDIS_USERNAME:${REDIS_USERNAME:default}}
app.redis.jedis.password=${SPRING_DATA_REDIS_PASSWORD:${REDIS_PASSWORD:}}
app.redis.jedis.ssl=${REDIS_SSL_ENABLED:false}

# === Monitoring / Actuator ===
management.endpoints.web.base-path=/health
management.endpoints.web.exposure.include=health,info,metrics,prometheus,env
management.endpoint.health.show-details=always
management.prometheus.metrics.export.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# === Logging ===
logging.level.root=INFO
logging.level.com.angerasilas.petroflow_backend=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO

# === Async & Scheduling ===
app.async.io.core-pool-size=${APP_ASYNC_IO_CORE_POOL_SIZE:8}
app.async.io.max-pool-size=${APP_ASYNC_IO_MAX_POOL_SIZE:16}
app.async.io.queue-capacity=${APP_ASYNC_IO_QUEUE_CAPACITY:500}
app.async.io.thread-name-prefix=${APP_ASYNC_IO_THREAD_NAME_PREFIX:pf-io-}
app.async.report.core-pool-size=${APP_ASYNC_REPORT_CORE_POOL_SIZE:4}
app.async.report.max-pool-size=${APP_ASYNC_REPORT_MAX_POOL_SIZE:8}
app.async.report.queue-capacity=${APP_ASYNC_REPORT_QUEUE_CAPACITY:200}
app.async.report.thread-name-prefix=${APP_ASYNC_REPORT_THREAD_NAME_PREFIX:pf-report-}
app.async.notification.core-pool-size=${APP_ASYNC_NOTIFICATION_CORE_POOL_SIZE:4}
app.async.notification.max-pool-size=${APP_ASYNC_NOTIFICATION_MAX_POOL_SIZE:8}
app.async.notification.queue-capacity=${APP_ASYNC_NOTIFICATION_QUEUE_CAPACITY:500}
app.async.notification.thread-name-prefix=${APP_ASYNC_NOTIFICATION_THREAD_NAME_PREFIX:pf-notify-}
app.async.sync.core-pool-size=${APP_ASYNC_SYNC_CORE_POOL_SIZE:4}
app.async.sync.max-pool-size=${APP_ASYNC_SYNC_MAX_POOL_SIZE:8}
app.async.sync.queue-capacity=${APP_ASYNC_SYNC_QUEUE_CAPACITY:300}
app.async.sync.thread-name-prefix=${APP_ASYNC_SYNC_THREAD_NAME_PREFIX:pf-sync-}
app.scheduling.pool-size=${APP_SCHEDULING_POOL_SIZE:4}
app.scheduling.thread-name-prefix=${APP_SCHEDULING_THREAD_NAME_PREFIX:pf-scheduler-}

# === CORS ===
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8081,http://192.168.2.100:8081,https://snzkpkvg-8081.uks1.devtunnels.ms,http://localhost:4173}

# === JWT ===
jwt.secret=${JWT_SECRET:dev-secret-key-change-in-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# === Mail ===
spring.mail.host=${SPRING_MAIL_HOST:${MAIL_HOST:smtp.gmail.com}}
spring.mail.port=${SPRING_MAIL_PORT:${MAIL_PORT:587}}
spring.mail.username=${SPRING_MAIL_USERNAME:${MAIL_USERNAME:}}
spring.mail.password=${SPRING_MAIL_PASSWORD:${MAIL_PASSWORD:}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# === Error Handling ===
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

